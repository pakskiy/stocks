server:
  port: 8080
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/stocks
    username: postgres
    password: 123456
    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    generate-ddl: true
    ddl-auto: update
  liquibase:
    change-log: classpath:liquibase/scripts.xml
logging:
  level:
    liquibase: ERROR
    root: INFO